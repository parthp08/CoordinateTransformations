language: python

python:
  - 3.6

before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
  - pip install codecov
  - pip install pytest-cov

install:
  - pip install ".[test]"

scrpit:
  - pytest

after_success:
  - codecov
